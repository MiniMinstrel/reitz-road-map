import React, { useState } from 'react';

const Search = (props) => {

	// Here's the search bar. All this needs to be is your
	// basic search bar. I can figure out the logic behind the
	// search, but you're welcome to try and implement it too if
	// you want. 

	return(
		<div>
			<p>Search!</p>
		</div>
		)
}

export default Search;